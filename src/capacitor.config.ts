import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.iysi_99999955.elmath',
  appName: 'el Math',
  webDir: 'build'
};

export default config;
